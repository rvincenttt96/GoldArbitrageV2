# تحلیل فنی GoldArbitrageV2

تحلیل روی کامیت `421c87d` ("GoldArbitrageV2 complete working version") انجام شده — تنها کامیت مخزن.
تاریخ بررسی: ۲۰۲۶-۰۸-۳۱. همهٔ اعداد قیمتی این سند از فراخوانی زندهٔ API‌های عمومی در همان لحظه گرفته شده‌اند.

---

## ۱. خلاصهٔ اجرایی

ربات کار می‌کند و واقعاً معاملهٔ سودده انجام داده — لاگ `real_trade_once.log` یک آربیتراژ کامل‌شدهٔ
`miligold → wallgold` روی ۰.۵ گرم با سود خالص تخمینی ۱۰۹,۶۲۰ تومان را ثبت کرده و پاسخ‌های واقعی هر دو
صرافی هم موجود است. پس این یک اسباب‌بازی نیست؛ یک سیستم معاملاتی زندهٔ در حال کار است.

اما سه دستهٔ مشکل جدی دارد:

| دسته | شدت | خلاصه |
|---|---|---|
| امنیت | بحرانی | توکن تلگرام، رمز گلدیکا، توکن والگلد و کد ملی داخل کد و روی گیت‌هاب عمومی هستند و **فعال‌اند** |
| صحت مدل آربیتراژ | بالا | میلی‌گلد قیمت خرید و فروشش یکی است؛ مدل کارمزد تخمینی است نه واقعی؛ هیچ قفل قیمتی وجود ندارد |
| مهندسی | متوسط تا بالا | fail-open در تأیید سفارش، اسکن ترتیبی و کند، بدون Decimal، بدون تست، بدون state، ۳۴۹۳ فایل ترک‌شده |

مهم‌ترین نکتهٔ تخصصی: **سیستم فعلاً آربیتراژ دو-پایهٔ ترتیبی می‌کند، در حالی که ترازنامهٔ خودتان
اجازهٔ آربیتراژ pre-funded هم‌زمان را می‌دهد.** این تنها تغییری است که ریسک اجرا را به‌شکل بنیادی کم می‌کند.

---

## ۲. معماری فعلی

```
live_bot.py  (حلقهٔ while True، ۳۴۰ خط، بدون تابع، credentials هاردکد)
   │
   ├─ ExchangeRegistry ──> WallGoldClient / GoldikaClient / MilliGoldClient
   │
   ├─ MarketScanner.scan()        →  ۶ درخواست HTTP ترتیبی (۳ صرافی × ۲ سمت)
   ├─ OpportunityFinder.find()    →  انتخاب بهترین مسیر + مقدار
   ├─ RiskManager.check()         →  بررسی حد سود/مقدار/موجودی
   └─ Executor.execute()          →  preflight → buy leg → sell leg
```

کد مرده که باید حذف شود: `services/arbitrage.py` (`ArbitrageEngine`) و `services/fee_engine.py`
(`FeeEngine`). هیچ‌جا نمونه‌سازی نمی‌شوند و مدل کارمزدشان با `OpportunityFinder` **تناقض** دارد
(میلی‌گلد را اصلاً نمی‌شناسند و `SAFETY_MARGIN` را کم می‌کنند در حالی که مسیر واقعی نمی‌کند).

تنظیمات مرده در `config/settings.py`: `MIN_PROFIT_PERCENT`، `MAX_PLATFORM_CASH`، `MAX_PLATFORM_GOLD`
هیچ‌جا خوانده نمی‌شوند. `SAFETY_MARGIN` فقط در کد مرده استفاده می‌شود.

---

## ۳. امنیت — بحرانی

### ۳.۱ اعتبارنامه‌های زنده روی مخزن عمومی

`live_bot.py` خطوط ۳۹–۵۶:

```python
wallgold = WallGoldClient("7969175|1JKem5nz...")   # توکن API والگلد
goldika  = GoldikaClient("9362798093", "Rv6047484") # موبایل + رمز گلدیکا
miligold = MilliGoldClient("+989362798093", "Rv6047484", "2020182000336301")
```

همین رمز در `main.py` هم تکرار شده. `telegram_config.py` توکن ربات را دارد. من با یک فراخوانی
read-only تأیید کردم که توکن **هنوز فعال است** (`GOLD2WALL_bot`).

`.wrangler/cache/wrangler-account.json` هم شناسهٔ حساب Cloudflare و ایمیل شما را لو داده.

نکتهٔ مهم: کد ملی `2020182000336301` روی مخزن عمومی، خودش یک مسئلهٔ جدا از مسئلهٔ معاملاتی است.

**اقدام فوری، قبل از هر تغییر کد:** رمز گلدیکا و میلی‌گلد را عوض کنید، توکن API والگلد را باطل و
دوباره صادر کنید، توکن ربات تلگرام را با `/revoke` در BotFather بچرخانید، و توکن Cloudflare را هم
عوض کنید. چون همه در تنها کامیت مخزن هستند، حذف فایل کافی نیست — تاریخچه باید بازنویسی شود یا
مخزن از نو ساخته شود.

### ۳.۲ پروکسی تلگرام یک رله‌ی کاملاً باز است

`worker.js`:

```javascript
const telegramUrl = "https://api.telegram.org" + url.pathname + url.search;
const response = await fetch(telegramUrl, { method: request.method, headers: request.headers, body: request.body });
```

هیچ احراز هویتی، هیچ allow-list مسیری، هیچ محدودیت نرخی. هر کسی این URL را پیدا کند می‌تواند از آن
به‌عنوان پروکسی عمومی به API تلگرام استفاده کند. و چون توکن هم عمومی است، هر کسی می‌تواند در کانال
شما پیام جعلی بفرستد. اگر روزی سیگنال‌های تلگرام ورودی تصمیم‌گیری شوند، این مسیر تزریق فرمان است.

درست: توکن را به‌صورت Secret در Worker نگه دارید، از کلاینت اصلاً توکن نفرستید، فقط مسیر
`/sendMessage` را مجاز کنید، و یک هدر اشتراکی برای احراز هویت بگذارید.

### ۳.۳ تأثیر جانبی: کد تست به AST وابسته شده

`inspect_goldika_fee.py` و `test_milli_commission_scale.py` هر دو `live_bot.py` را با `ast.parse`
تجزیه می‌کنند تا credentials را از داخل سورس بیرون بکشند. این نشانهٔ مستقیم فساد ساختاری است:
چون رمز جای درستی نیست، ابزار جانبی مجبور شده سورس را پارس کند.

---

## ۴. تحلیل تخصصی آربیتراژ

### ۴.۱ میلی‌گلد اسپرد ندارد — و این کل روتر را منحرف می‌کند

این مهم‌ترین یافتهٔ این بررسی است.

`MilliGoldClient.get_price(side)` پارامتر `side` را می‌گیرد ولی **کاملاً نادیده می‌گیرد**:

```python
def get_price(self, side="buy"):
    raw_price = self._get_raw_price()          # همیشه data["price18"]
    price_per_gram_tmn = raw_price * 100       # مستقل از side
```

خروجی زندهٔ endpoint در لحظهٔ بررسی:

```json
{"code":0,"data":{"price18":217680,"date":"2026-08-31T17:03:39"}}
```

یک عدد. نه bid، نه ask. مقایسه کنید با گلدیکا که واقعاً دو طرفه است:

```json
{"data":{"price":{"id":415713,"buy":225082172,"sell":219744254}}}
```

پس در دید موتور، وضعیت لحظهٔ بررسی این بود (تومان بر گرم):

| صرافی | خرید | فروش | اسپرد |
|---|---:|---:|---:|
| miligold | ۲۱,۷۶۸,۰۰۰ | ۲۱,۷۶۸,۰۰۰ | **۰** |
| goldika | ۲۲,۵۰۸,۲۱۷ | ۲۱,۹۷۴,۴۲۵ | ۵۳۳,۷۹۲ |
| wallgold | دو طرفهٔ واقعی | | |

نتیجه: میلی‌گلد در نگاه موتور هم ارزان‌ترین جای خرید است و هم گران‌ترین جای فروش، چون روی mid
قیمت‌گذاری شده. به همین دلیل است که **تمام** فرصت‌های ثبت‌شده در `live_bot_runtime.log` و
`real_trade_once.log` بدون استثنا `buy_platform='miligold'` هستند. این نتیجهٔ تحلیل بازار نیست،
نتیجهٔ یک بایاس مدل است.

اسپرد واقعی میلی‌گلد در `calculatedCommission` است که از `/api/v1/trade/commission` می‌آید — و
`OpportunityFinder` **هرگز آن را صدا نمی‌زند**؛ فقط ثابت ۰.۵٪ می‌گذارد. کارمزد واقعی تنها داخل
`init_trade_request` گرفته می‌شود، یعنی وقتی که دیگر در حال اجرای معامله هستید.

### ۴.۲ فرصت طلایی که استفاده نشده: میلی‌گلد قیمت تضمینی ۳۰ ثانیه‌ای می‌دهد

از پاسخ واقعی خرید در `real_trade_once.log`:

```json
{"code":0,"data":{"isTimeValiditySupported":true,"timeValidityInSeconds":30,
                  "invoiceStatus":"DONE","invoiceUuid":"fd2cf0fd-..."}}
```

یعنی `init_trade_request` یک فاکتور با اعتبار ۳۰ ثانیه صادر می‌کند و `confirm_trade` آن را نهایی
می‌کند. این دقیقاً یک **firm quote** است. الان کد این دو را پشت سر هم و بدون فاصله صدا می‌زند
(`buy()` = init + confirm) و آن ۳۰ ثانیه را دور می‌ریزد.

طراحی درست:

1. اول روی میلی‌گلد `init_trade_request` بزنید → قیمت و کارمزد **دقیق** برای ۳۰ ثانیه قفل می‌شود.
2. با آن عدد قفل‌شده، حاشیهٔ سود را در برابر نرخ زندهٔ والگلد حساب کنید.
3. اگر هنوز سودده بود: پای والگلد را بزنید، بعد `confirm_trade` را بزنید.
4. اگر نبود: فاکتور را تأیید نکنید، خودش منقضی می‌شود.

این تغییر، تخمین را به قیمت قطعی تبدیل می‌کند و ریسک تک‌پایه‌ماندن را تقریباً حذف می‌کند. با
ساختار فعلی کد این کار شدنی است چون `init_trade_request` و `confirm_trade` از قبل جدا هستند.

### ۴.۳ ترتیب پایه‌ها: چرا اصلاً ترتیبی؟

`Executor.execute` همیشه اول می‌خرد بعد می‌فروشد. اگر پای فروش بخورد زمین، شما طلا روی دستتان مانده
و ربات `trading_halted = True` می‌شود و دستی باید نجاتش دهید. هیچ unwind خودکاری نیست.

ولی به موجودی واقعی خودتان در لاگ نگاه کنید:

```
BUY  PORTFOLIO: miligold  cash=11,263,249  gold=0.504
SELL PORTFOLIO: wallgold  cash=18,664,387  gold=5.199
```

شما روی هر دو صرافی هم نقد دارید هم طلا. یعنی لازم نیست پایه‌ها را زنجیره‌ای اجرا کنید. می‌توانید
هر دو پایه را **هم‌زمان** روی موجودی از پیش تأمین‌شده شلیک کنید و بعداً موجودی‌ها را rebalance کنید.
این استاندارد آربیتراژ خرده‌فروشی طلا است و پنجرهٔ ریسک را از چند ثانیه به تقریباً صفر می‌رساند.

ضمناً پای میلی‌گلد الان خودش ۴ رفت‌وبرگشت HTTP است (`_get_raw_price` + `get_commission` + init + confirm)
و بعدش تازه نوبت والگلد می‌شود. فاصلهٔ زمانی بین دو پایه به‌راحتی چند ثانیه است. برای آربیتراژ این خیلی زیاد است.

### ۴.۴ حد سود مطلق است، نه درصدی

`MIN_NET_PROFIT = 40000` تومان — عدد ثابت، بدون توجه به حجم:

| مقدار | ارزش معامله | ۴۰,۰۰۰ تومان یعنی |
|---:|---:|---:|
| ۰.۱ گرم | ۲,۱۷۶,۸۰۰ | ۱.۸۴٪ بازده |
| ۰.۵ گرم | ۱۰,۸۸۴,۰۰۰ | ۰.۳۷٪ بازده |

یعنی همان آستانه در حجم بزرگ خیلی سهل‌گیرانه و در حجم کوچک خیلی سخت‌گیرانه است. `MIN_PROFIT_PERCENT`
دقیقاً برای همین در `settings.py` تعریف شده — ولی هیچ‌جا خوانده نمی‌شود. باید هر دو شرط با هم اعمال شوند.

### ۴.۵ شکست کارمزد والگلد در ۰.۴ گرم — تابع سود یکنواخت نیست

`get_sell_fee` برای والگلد حداقل مبلغ مشمول را ۰.۴ گرم می‌گیرد. اجرای واقعی این را نشان می‌دهد:

| مقدار | کارمزد فروش | نرخ مؤثر |
|---:|---:|---:|
| ۰.۱ گرم | ۴۳,۶۴۰ | **۲.۰۰۰٪** |
| ۰.۲ گرم | ۴۳,۶۴۰ | ۱.۰۰۰٪ |
| ۰.۳ گرم | ۴۳,۶۴۰ | ۰.۶۶۷٪ |
| ۰.۴ گرم | ۴۳,۶۴۰ | ۰.۵۰۰٪ |
| ۰.۵ گرم | ۵۴,۵۵۰ | ۰.۵۰۰٪ |

پیامد: `MIN_TRADE_AMOUNT = 0.1` درست وسط منطقهٔ جریمه است. هر فروش زیر ۰.۴ گرم روی والگلد چهار برابر
نرخ اسمی هزینه دارد. عدد ۰.۴ هم به‌صورت magic number داخل تابع نشسته، نه در `settings.py`.

این باعث می‌شود سود نسبت به حجم **غیریکنواخت** باشد — در شبیه‌سازی من سود در ۰.۲ گرم
(−۵۵,۰۰۸) از سود در ۰.۳ گرم (−۴۹,۸۰۸) بدتر بود. پس جست‌وجوی مقدار واقعاً لازم است، ولی روش فعلی
(`_candidate_amounts` میلی‌گرم‌به‌میلی‌گرم) در هر `find()` عملاً **۹۶۶ بار** `_build_opportunity` را
صدا می‌زند. الان ارزان است (۱.۴ میلی‌ثانیه) ولی ساختار مسئله را پنهان می‌کند: کافی است فقط
نقاط شکست کارمزد و کران‌های موجودی را ارزیابی کنید.

### ۴.۶ گلدیکا: سه واحد مختلف روی یک صرافی

در `adapters/goldika/client.py` سه واحد متفاوت برای مقدار طلا استفاده می‌شود:

- `buy()` → `'amount': amount` (گرم خام)
- `sell()` → `'amount': int(amount * 100)` (صدم گرم)
- موجودی در `live_bot.py` → `gold_milli / 1000` (میلی‌گرم)

اگر API خرید هم صدم‌گرم بخواهد، `buy(0.5)` در واقع ۰.۰۰۵ گرم می‌خرد. وجود کلید کشتار
`GOLDIKA_BUY_ENABLED` و اسکریپت `enable_goldika_buy.py` و `debug_goldika_amounts.py` (که مقدار را
به‌صورت رشته `"0.001"` تا `"0.500"` پروب می‌کند) نشان می‌دهد این موضوع هیچ‌وقت قطعی حل نشده.

**تا وقتی این با یک معاملهٔ کوچک واقعی تأیید نشده، پای خرید گلدیکا نباید فعال شود.**

نکتهٔ دوم: گلدیکا اسپرد ۲.۴٪ در قیمت دارد و کد **علاوه بر آن** ۱.۲٪ کارمزد هم کم می‌کند. اگر
آن ۱.۲٪ داخل خود قیمت باشد، دارید دوبار حساب می‌کنید و گلدیکا مصنوعاً همیشه بی‌جاذبه می‌شود.
فیلد `discounted_price` که API برمی‌گرداند اصلاً خوانده نمی‌شود. این باید با
`/api/v1/exchanges?page=1` (تاریخچهٔ واقعی معاملات) کالیبره شود.

### ۴.۷ بدون قفل قیمت، بدون بررسی کهنگی

`WallGoldClient.get_price` فیلدهای واقعی `priceExpiresAt` و `ttl` را می‌گیرد و در `Quote` می‌گذارد —
و بعد **هیچ‌کس هرگز آنها را چک نمی‌کند**. گلدیکا بدتر: `expires_at=datetime.now()` و `ttl=30`
هاردکد شده، یعنی فیلد اصلاً بی‌معنی است.

همچنین `buy()` در گلدیکا و میلی‌گلد داخل خودش دوباره قیمت می‌گیرد، در حالی که `Executor` همین الان
در preflight گرفته بود. یعنی قیمتی که سودآوری بر اساس آن تأیید شده، **قیمتی نیست که سفارش با آن ثبت می‌شود**.

### ۴.۸ بدون تطبیق پس از معامله

`WallGoldClient.get_order` نوشته شده ولی **هیچ‌جا صدا زده نمی‌شود**. سود گزارش‌شده همیشه
`estimated_net_profit` است. پاسخ خرید میلی‌گلد اصلاً قیمت اجراشده را برنمی‌گرداند، پس هزینهٔ واقعی
پای خرید هرگز تأیید نمی‌شود. یعنی نمی‌دانید P&L واقعی‌تان چقدر است.

از لاگ می‌شود پسینی حسابش کرد: نقد میلی از ۱۱,۲۶۳,۲۴۹ به ۵۱۸,۹۴۳ رفت (−۱۰,۷۴۴,۳۰۶) و نقد والگلد
از ۱۸,۶۶۴,۳۸۷ به ۲۹,۵۲۸,۷۹۲ (+۱۰,۸۶۴,۴۰۵) → سود واقعی ۱۲۰,۰۹۹. آن بار تخمین درست بود. ولی این
باید کار خود سیستم باشد، نه کار دستی از روی لاگ.

---

## ۵. مهندسی پایتون

### ۵.۱ بحرانی: `_response_ok` به‌صورت fail-open طراحی شده

`services/executor.py` خطوط ۱۵–۷۳. اگر پاسخ هیچ‌کدام از کلیدهای `success` / `code` / `status` /
`error` / `errors` را نداشته باشد:

```python
return True
```

یک سیستم معاملاتی هرگز نباید پاسخ ناشناخته را موفق فرض کند. سناریوی واقعی: گلدیکا
`{"message": "موجودی کافی نیست"}` برگرداند → `_response_ok` می‌گوید True → موتور خرید را موفق
فرض می‌کند → پای فروش را روی صرافی دیگر اجرا می‌کند → شما طلایی را فروخته‌اید که هرگز نخریده‌اید.

باید برعکس باشد: هر شکل پاسخی که صریحاً به‌عنوان موفقیت شناخته نشده، شکست است. و بهتر از آن، هر
آداپتور خودش پاسخ خودش را پارس کند و یک `OrderResult` تایپ‌دار برگرداند — نه اینکه `Executor` سعی
کند شکل پاسخ سه API متفاوت را حدس بزند.

### ۵.۲ حلقهٔ اصلی همه چیز را می‌بلعد

```python
except Exception as e:
    print("ERROR:", e)
...
time.sleep(5)
```

این `except` عریان انقضای توکن، خطای احراز هویت، صفحهٔ HTML به‌جای JSON، و باگ‌های منطقی را
یکسان رفتار می‌کند و بی‌صدا ادامه می‌دهد. در لاگ واقعی هر دو حالت دیده می‌شود:

```
ERROR: 'charmap' codec can't encode characters in position 215-220
ERROR: Expecting value: line 1 column 1 (char 0)
```

اولی از همه بدتر است: یک معاملهٔ **واقعاً اجراشده** بود که موقع لاگ‌کردن نتیجه‌اش خطای انکودینگ
داد، پس نتیجه هرگز ثبت نشد و تلگرام هرگز خبر نداد. بدترین حالت ممکن در یک سیستم معاملاتی: پوزیشن
باز بدون رکورد. (نسخهٔ فعلی `reconfigure` را اضافه کرده، ولی الگوی معماری هنوز همان است.)

هیچ re-login خودکاری هم وجود ندارد: وقتی توکن منقضی شود، حلقه تا ابد ۴۰۱ می‌گیرد و هر ۵ ثانیه
`print` می‌کند.

### ۵.۳ پول با `float` حساب می‌شود

`amount: float` و `price: int` و سود `float`. برای پول باید `Decimal` استفاده شود. ضمناً
`int(price.price * int(amount * 100) // 100)` در پی‌لود فروش گلدیکا تقسیم صحیح دارد که مبلغ کل را
به سمت پایین گرد می‌کند و می‌تواند باعث رد شدن سفارش شود.

### ۵.۴ `datetime` ناسازگار

`ruff` روی ۳۰ فایل واقعی سورس، ۳۲ مورد پیدا کرد. مهم‌ترینشان:

- `MarketScanner` از `datetime.utcnow()` استفاده می‌کند (naive و از پایتون ۳.۱۲ منسوخ).
- کوت‌های والگلد timezone-aware هستند (`fromisoformat` روی `Z`).
- کوت‌های گلدیکا و میلی‌گلد از `datetime.now()` بدون tz می‌آیند.

سه نوع datetime در یک مدل داده. هر مقایسه‌ای بین aware و naive استثنا می‌دهد — و چون هیچ‌جا
کهنگی بررسی نمی‌شود، این باگ فعلاً خفته است و روزی که بخواهید TTL را چک کنید بیدار می‌شود.

### ۵.۵ بدون تست، بدون لاگر، بدون state

- ۳۱ فایل `test_*.py` وجود دارد ولی هیچ‌کدام تست نیستند — اسکریپت‌های دستی‌اند که به شبکهٔ زنده و
  حساب واقعی وصل می‌شوند. `test_compare_milligold.py` اصلاً خراب است (`MilliGoldClient` را با ۲
  آرگومان صدا می‌زند در حالی که ۳ تا لازم دارد، و `Quote` را مثل dict استفاده می‌کند).
- هیچ استفاده‌ای از `logging` نیست؛ همه‌جا `print`.
- `trading_halted` فقط در حافظه است. ری‌استارت کنید، پاک می‌شود — حتی اگر پوزیشن نیمه‌باز داشته باشید.
- منطق halt در `live_bot.py` دو بار تکرار شده (خطوط ۲۸۴–۲۹۲ و ۳۲۱–۳۳۱).
- `real_trade_once.py` (۴۲۰ خط) عملاً کپی منطق `live_bot.py` است.
- بدون idempotency key روی سفارش‌ها. اگر روزی retry اضافه شود، معاملهٔ دوبل می‌دهد.
- بدون حالت paper/dry-run در حلقهٔ اصلی — همیشه زنده است.
- `requirements.txt` فقط `requests` و `python-dotenv` بدون هیچ نسخه‌ای دارد.

---

## ۶. بهداشت مخزن

| مورد | تعداد |
|---|---:|
| کل فایل ترک‌شده | ۳,۴۹۳ |
| از این تعداد `node_modules` | ۳,۲۷۳ |
| فایل backup / `.bak` | ۶۳ |
| اسکریپت `patch_*.py` | ۱۸ |
| اسکریپت `debug_*.py` | ۱۸ |
| اسکریپت `test_*.py` | ۳۱ |
| فایل `.pyc` | ۱۶ |
| state داخلی `.wrangler` | ۱۹ |
| **سورس واقعی** | **۳۰** |

حجم مخزن ۴۳۱ مگابایت است که ۳۷۳ مگابایتش `telegram-proxy/` است. `.git` هم ۵۶ مگابایت.

اسکریپت‌های `patch_*.py` الگوی نگران‌کننده‌ای دارند — ویرایش سورس با regex در زمان اجرا:

```python
s, n = re.subn(r'(?m)^GOLDIKA_BUY_ENABLED\s*=\s*.*$', 'GOLDIKA_BUY_ENABLED = True', s)
```

اینها جایگزین کنترل نسخه شده‌اند. `.gitignore` هم `debug_*.py` و `test_*.py` را نادیده می‌گیرد
ولی چون قبلاً ترک شده بودند، بی‌اثر است. و کل پروژه یک کامیت دارد، پس هیچ تاریخچه‌ای برای بازگشت نیست.

---

## ۷. تغییرات پیشنهادی، به ترتیب اولویت

### فوری (قبل از هر کد جدید)
1. چرخاندن **همهٔ** اعتبارنامه‌ها: گلدیکا، میلی‌گلد، والگلد، ربات تلگرام، Cloudflare.
2. مخزن را خصوصی کنید و تاریخچه را پاک کنید (کد ملی هم داخلش است).
3. انتقال همه‌چیز به `.env` + `pydantic-settings`، حذف `telegram_config.py`.
4. بستن پروکسی Worker: توکن به‌صورت Secret، allow-list مسیر، هدر احراز هویت.

### صحت مدل (بیشترین اثر روی سود)
5. `MilliGoldClient.get_price` باید کارمزد واقعی را از `/trade/commission` بگیرد و قیمت خرید و
   فروش را از هم جدا کند. تا آن موقع، مدل روتر شما سوگیری ساختاری دارد.
6. اجرای مبتنی بر فاکتور برای میلی‌گلد: `init → verify → execute other leg → confirm`،
   با استفاده از همان پنجرهٔ ۳۰ ثانیه‌ای که الان دور ریخته می‌شود.
7. کالیبره کردن مدل کارمزد گلدیکا با تاریخچهٔ واقعی معاملات؛ روشن کردن اینکه ۱.۲٪ روی قیمت
   سوار است یا جداست.
8. اضافه کردن `MIN_PROFIT_PERCENT` در کنار `MIN_NET_PROFIT` و بردن حداقل ۰.۴ گرم والگلد به `settings.py`.

### ایمنی اجرا
9. `_response_ok` باید fail-closed شود؛ هر آداپتور `OrderResult` تایپ‌دار برگرداند.
10. بررسی کهنگی کوت با `expires_at` و `ttl` واقعی قبل از هر سفارش.
11. تطبیق پس از معامله با `get_order` و ثبت P&L واقعی، نه تخمینی.
12. state پایدار (SQLite یا JSON) برای `trading_halted`، سفارش‌های در جریان و تاریخچهٔ معاملات.
13. اجرای هم‌زمان دو پایه روی موجودی pre-funded به‌جای زنجیرهٔ خرید-سپس-فروش.

### مهندسی
14. `Decimal` برای پول، datetime یکدست و aware.
15. `logging` ساخت‌یافته به‌جای `print`؛ حذف `except Exception` عریان از حلقهٔ اصلی.
16. تبدیل `live_bot.py` از اسکریپت خطی به توابع + `main()`؛ ادغام `real_trade_once.py`.
17. اسکن هم‌زمان به‌جای ۶ درخواست ترتیبی؛ چرخهٔ کوتاه‌تر از ۵ ثانیه.
18. تست واقعی pytest با آداپتورهای mock و ثابت‌های ثبت‌شده از پاسخ‌های واقعی API.
19. حذف کد مرده: `services/arbitrage.py`، `services/fee_engine.py`، تنظیمات بی‌استفاده.
20. تمیزکاری مخزن: `node_modules`/`.wrangler`/`__pycache__`/backup‌ها/`patch_*` خارج شوند، `.gitignore` درست شود.
